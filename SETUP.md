# نقل الموقع لأبنائك — دليل الإعداد

هذا الموقعُ صفحاتُ HTML ساكنة، تُستضاف مجّانًا على GitHub Pages، وتحفظ
النتائجَ في قاعدةِ بياناتٍ مجّانيّة. لا خادمَ ولا اشتراك.

> **قبل كلِّ شيء:** لا تستعمل قاعدةَ بياناتِ صاحبِ النسخةِ الأصليّةِ ولا
> مفتاحَه. لو تركتَهما كما هما فنتائجُ أبنائك تنزل في جدولِ غيرِك مخلوطةً
> بأبنائه، وكلٌّ منكما يرى نتائجَ الآخر. والخطواتُ الثلاثُ الأولى أدناه
> تمنع ذلك، وهي لا تأخذ نصفَ ساعة.

---

## ١) قاعدة البيانات (Supabase — مجّاني)

1. افتح [supabase.com](https://supabase.com) → أنشئ حسابًا → **New project**.
2. من **SQL Editor** نفّذ هذا السطرَ لإنشاء الجدول:

```sql
create table attempts (
  id          bigserial primary key,
  created_at  timestamptz default now(),
  student     text,
  grade       text,
  en_correct  int, en_total int,
  ma_correct  int, ma_total int,
  wrong       jsonb,
  meta        jsonb
);

alter table attempts enable row level security;

-- الأبناء يكتبون نتائجهم، والأب يقرؤها. والمفتاحُ القابلُ للنشر ظاهرٌ
-- في الصفحة بطبيعته، فالحمايةُ هنا لا في إخفائه.
create policy "insert" on attempts for insert to anon with check (true);
create policy "read"   on attempts for select to anon using (true);
```

3. من **Settings → API** انسخ:
   - **Project URL**
   - **Publishable key** (أو `anon public` في المشاريع الأقدم)

4. ضعهما في **`config.js`**:

```js
window.APP_CONFIG = {
  supaUrl: "https://xxxxx.supabase.co",
  supaKey: "sb_publishable_xxxxx"
};
```

هذا الملفُّ وحدَه — لا تبحث عن المفتاحِ في الصفحات، فكلُّها تقرأ منه.

---

## ٢) كلمة المرور

الموقعُ عامٌّ على الإنترنت، وقفلُه كلمةُ مرورٍ واحدة. غيّرها:

1. احسب SHA-256 لكلمتك. من الطرفيّة:
   ```bash
   printf '%s' 'كلمتك' | sha256sum
   ```
   أو من متصفّحك: افتح صفحةَ الموقعِ ثمّ Console والصق:
   ```js
   crypto.subtle.digest('SHA-256', new TextEncoder().encode('كلمتك'))
     .then(b => console.log([...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')));
   ```
2. ضع الناتجَ في `PW_HASH` داخل **`gate.js`**.

> وهذا قفلُ رابطٍ لا حمايةَ حقيقيّة: يمنع من يفتح الرابطَ صدفةً، ولا يمنع
> من يقرأ شيفرةَ الصفحة. فلا تضع فيه ما لا تريد أن يُرى.

---

## ٣) تقييم النطق (Azure — اختياريّ)

إن لم تُرِدْه فاترك **`azure-config.js`** فارغًا، ويعمل الموقعُ بلا قسمِ النطق.

وإن أردتَه:
1. حسابُ Microsoft **شخصيّ** (بريدُ وزارةِ التعليم لا يصلح).
2. [portal.azure.com](https://portal.azure.com) → Create a resource → **Speech** → Pricing tier **Free F0** (خمس ساعاتٍ شهريًّا مجّانًا).
3. Keys and Endpoint → انسخ **KEY 1** و**Region**، وضعهما في `azure-config.js`.

> **مفتاحُ Azure سرٌّ حقيقيّ** بخلافِ مفتاحِ Supabase: من أخذه استهلك
> حصّتَك. ولأنّ المستودعَ عامٌّ فالمفتاحُ فيه مقروءٌ لكلِّ أحد — فإن
> استعملتَه فراقب استهلاكَك، وبدّله إذا رأيتَ ما لا تعرف.

---

## ٤) الأبناء

أسماءُ الأبناءِ مكتوبةٌ في الصفحاتِ نفسِها (خرائطُ `NAMES` و`GRADES`).
ابحث عن `NAMES = {` وبدّل ما فيها بأسمائك وصفوفهم، ثمّ احذف من
`home.html` بطاقاتِ الموادّ التي لا تناسب أبناءك.

واحذف مجلّدَ **`notes/`** — فيه ملاحظاتُ أبناءِ صاحبِ النسخةِ الأصليّة.

---

## ٥) النشر

ارفع الملفّاتِ إلى مستودعٍ على GitHub، ثمّ **Settings → Pages** واختر
الفرعَ `main`. يصير الموقعُ على `https://<اسمك>.github.io/<المستودع>/`.

---

## ما الذي يعمل بلا إعداد؟

الأسئلةُ كلُّها مولَّدةٌ في المتصفّح: القدراتُ والتحصيليُّ وستيب
والكانجرو وصفحاتُ المنهج. تعمل بمجرّدِ فتحِ الصفحةِ ولو بلا قاعدةِ
بيانات — والقاعدةُ إنّما تحفظ النتائجَ لتظهر في لوحةِ المتابعة.

## ما الذي يحتاج تعديلًا لمنهجٍ مختلف؟

صفحاتُ المنهج (`math4` … `math9`, `science*`, `lughati*`, `english*`)
مبنيّةٌ على كتبٍ بعينِها. لو كان أبناؤك في صفوفٍ أخرى فإمّا أن تحذفها
أو تُعيد بناءَ بنوكِها. أمّا القدراتُ والتحصيليُّ وستيب فعامّةٌ لا
تتعلّق بصفٍّ ولا كتاب.
